# Redirigir HTTP 80 → HTTPS en 8443
server {
    listen 80;
    server_name sigepaa-aa.com www.sigepaa-aa.com dev.sigepaa-aa.com;

    return 301 https://$host:8443$request_uri;
}

# API principal en 8443
server {
    listen 8443 ssl;
    server_name sigepaa-aa.com www.sigepaa-aa.com;

    ssl_certificate /root/certs/fullchain.cer;
    ssl_certificate_key /root/certs/sigepaa-aa.com_private_key.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    client_max_body_size 200M;

    location / {
        proxy_pass http://100.108.186.34:3005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }
}

# API developers en 8445
server {
    listen 8443 ssl;
    server_name dev.sigepaa-aa.com;

    ssl_certificate /root/certs/fullchain.cer;
    ssl_certificate_key /root/certs/sigepaa-aa.com_private_key.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    client_max_body_size 200M;

    location / {
        proxy_pass http://100.108.186.34:4005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }
}

# WEB EPAA
server {
    listen 8443 ssl;
    server_name web.sigepaa-aa.com;

    ssl_certificate /root/certs/fullchain.cer;
    ssl_certificate_key /root/certs/sigepaa-aa.com_private_key.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    client_max_body_size 200M;

    location / {
        proxy_pass http://100.108.186.34:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }
}

